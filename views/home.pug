extends ./layout/index

block slider
    .slider
        h1 En el Mejor Lugar
        em Los Mejores Videojuegos

block content
    main(class='my-10')
        h2(class="text-4xl text-center text-gray-300 font-semibold font-staatliches mb-10") Sobre Nosotros
        div(class='flex flex-col lg:flex-row items-center justify-center gap-10 my-10') 
            div(class='w-4/5 lg:w-1/3')
                p.text-gray-300 ¡Bienvenidos a GameHaven! En GameHaven, vivimos y respiramos videojuegos. Somos un equipo de apasionados gamers y críticos que se dedica a ofrecerte la documentación más completa y detallada del universo de los videojuegos. Ya sea que estés buscando información sobre el último lanzamiento, revisando las calificaciones de los clásicos atemporales o comparando precios para tu próxima compra, ¡lo tenemos todo cubierto! Descubre de qué se tratan tus videojuegos favoritos con descripciones detalladas y análisis profundos, lee reseñas auténticas de jugadores y críticos, y encuentra las mejores ofertas comparando precios de diferentes plataformas.
            figure(class="w-4/5 lg:w-[600px]")
                img(src="/img/mando.webp" alt="Imagen sobre nosotros" class="w-full h-full object-cover rounded-md shadow-lg shadow-black/60")

        div(class='flex flex-col items-center justify-center gap-4')
            div(class='w-4/5 xl:w-full flex flex-col lg:flex-row items-center flex-wrap justify-center lg:items-stretch gap-4 xl:my-10')
                include ./partials/games_list
            h2(class="text-4xl text-center text-gray-300 font-semibold font-staatliches my-5") Reseñas
            div(class='w-4/5 lg:w-full flex flex-col lg:flex-row justify-center gap-4 items-center lg:items-stretch lg:my-10')
                include ./partials/reviews_list

    script.
        document.addEventListener('DOMContentLoaded', () => {
            const openModalButtons = document.querySelectorAll('.open-modal');
            const closeModalButtons = document.querySelectorAll('.close');
            const modals = document.querySelectorAll('.modal');

            openModalButtons.forEach(button => {
                button.addEventListener('click', () => {
                    const modalId = button.getAttribute('data-modal-target');
                    document.getElementById(modalId).classList.remove('hidden');
                });
            });

            closeModalButtons.forEach(button => {
                button.addEventListener('click', () => {
                    button.closest('.modal').classList.add('hidden');
                });
            });

            window.addEventListener('click', (event) => {
                if (event.target.classList.contains('modal')) {
                    event.target.classList.add('hidden');
                }
            });
        });